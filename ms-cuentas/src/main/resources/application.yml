server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: ms-cuentas

  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:db_cuentas}
    username: ${DB_USER:postgres}
    password: ${DB_PASS:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  rabbitmq:
    host: ${RABBIT_HOST:localhost}
    port: ${RABBIT_PORT:5672}
    username: ${RABBIT_USER:guest}
    password: ${RABBIT_PASS:guest}

rabbitmq:
  exchange: sofka.exchange
  queue:
    cliente-creado: cliente.creado.queue
    cliente-eliminado: cliente.eliminado.queue
  routing-key:
    cliente-creado: cliente.creado
    cliente-eliminado: cliente.eliminado

logging:
  level:
    com.sofka: DEBUG
    org.springframework.amqp: INFO
